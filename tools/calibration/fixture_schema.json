{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Calibration Fixture",
  "description": "Labeled capture outcome for heuristic calibration evaluation",
  "type": "object",
  "required": ["fixture_id", "label", "coverage_report"],
  "properties": {
    "fixture_id": {
      "type": "string",
      "description": "Unique ID for this fixture (e.g. 'fix_001')"
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the capture scenario"
    },
    "label": {
      "type": "string",
      "enum": ["good", "recapture", "bad", "noisy", "insufficient"],
      "description": "Ground truth outcome label"
    },
    "coverage_report": {
      "type": "object",
      "description": "Synthetic or real coverage_report dict from CoverageAnalyzer.analyze_coverage()"
    },
    "validation_report": {
      "type": "object",
      "description": "Optional synthetic or real validation_report dict from AssetValidator.validate()"
    },
    "notes": {
      "type": "string",
      "description": "Any additional context about this fixture"
    }
  }
}
